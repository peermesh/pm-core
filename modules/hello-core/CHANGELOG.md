# Changelog

## Unreleased

- Add the Hello Core Module: a near-stock in-runtime copy of the standalone hello module example
  with unique module/service names, config keys (`HELLO_CORE_*`), and Traefik labels so it can
  coexist with other hello-style modules in the runtime.
