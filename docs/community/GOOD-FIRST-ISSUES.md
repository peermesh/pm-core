# Good First Issues Backlog

1. Add automated check that `.github/ISSUE_TEMPLATE/*.yml` remains valid YAML in CI.
2. Add docs linter target for `docs/community/` markdown consistency.
3. Add `just quickstart-check` wrapper for the tutorial commands.
4. Add a lightweight script to verify required env keys are present before deploy.
5. Add smoke test for issue-template links and security advisory URL.
6. Add public doc cross-reference check for `README.md` -> community docs.
