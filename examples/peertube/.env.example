# ==============================================================
# PeerTube Environment Configuration
# ==============================================================

DOMAIN=example.com
PEERTUBE_HOSTNAME=peertube.${DOMAIN}

# Optional admin override
PEERTUBE_ADMIN_EMAIL=admin@${DOMAIN}

# Required secrets (in encrypted bundle and/or secrets/ files):
# - peertube_db_password
# - peertube_secret
