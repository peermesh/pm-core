# Docker Lab Design Requirements

If possible, we need to be able to have two VPSs or any number of VPSs and add Docker container sub modules to either one and have them work together. problem of having a VPS reach its capacity with a new module being added and instead of having to scale up to the next biggest server you would add additional servers and link them this is all part of the modular design 

# ai modules in sandboxed environments

