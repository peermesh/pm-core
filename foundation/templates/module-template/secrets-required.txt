# Module Template - Required Secrets
#
# List secret files that must be created before deployment.
# Generate with: openssl rand -base64 32 > secrets/<filename>
# Set permissions: chmod 600 secrets/<filename>
#
# CUSTOMIZE: Replace with your module's actual secret requirements.
#
# Example:
# secrets/my_module_api_key
# secrets/my_module_db_password
